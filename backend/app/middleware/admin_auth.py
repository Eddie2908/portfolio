# This middleware is NOT used — admin authentication is handled via
# FastAPI dependencies in app/api/dependencies.py (get_admin_user).
# Keeping this file as reference only. Do not register in main.py.
